import SwiftUI

struct PopoverContent: View {
    var body: some View {
        VStack {
            Text("Popover")
                .font(.title)
        }
        .padding()
        .frame(width: 200)
    }
}
